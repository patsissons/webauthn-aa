# CLAUDE.md

This project's contributor guidance for AI agents lives in
[`AGENTS.md`](AGENTS.md). Read it before making changes.

Most important: after completing any work, run `pnpm validate` (Prettier check →
typecheck → lint → unit tests → e2e tests) and fix any regressions before
considering the task done. Run `pnpm format` after writing or editing files.
